import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "rubrik-new, sans-serif",
    body: "rubrik-new, sans-serif",
  },
  colors: {},
});

export default theme;
